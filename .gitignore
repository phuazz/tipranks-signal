# --- IP firewall: vendor-derived values NEVER enter version control ---
# TipRanks (Ultimate, personal-use) and Norgate (Platinum, personal-use) data.
# Raw exports live in OneDrive; snapshots / merges / caches stay local-only.
data/
*.xlsx
*.xls

# Filed study records and diagnostics stay local. These are working research
# output, not publication: the repo publishes code, docs and the aggregate
# Pages surfaces, and the studies ledger points at these files on disk.
reviews/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/
